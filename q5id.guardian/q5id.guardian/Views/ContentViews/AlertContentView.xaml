<?xml version="1.0" encoding="UTF-8" ?>
<base:BaseContainerView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:base="clr-namespace:q5id.guardian.Views.Base"
    x:Class="q5id.guardian.Views.ContentViews.AlertContentView">
    <ContentView.Content>
        <Grid>
            <Grid Margin="0" Padding="0" x:Name="GridContentView">
                
                              
            </Grid>
            <Grid IsVisible="{Binding IsLoading}" BackgroundColor="{StaticResource lightCharcoal10}">
                <ActivityIndicator
                    IsRunning="true"
                    WidthRequest="100"
                    HeightRequest="100"
                    HorizontalOptions="Center"
                    VerticalOptions="Center"
                    Color="{StaticResource redNue}" />
            </Grid>
        </Grid>
    </ContentView.Content>
</base:BaseContainerView>
